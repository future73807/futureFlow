# futureFlow UI 重构状态

更新时间：2026-07-25

## 目标

将产品从混搭式演示界面重构为一套统一、可操作、可验证的工作流产品界面，并确保画布、弹窗、个人中心、工作流列表和网关执行流程在真实浏览器中可用。

## 已完成

### 全局体验

- 重做全局视觉令牌和 Semi UI 覆盖样式，收敛颜色、边距、圆角、排版和按钮层级。
- 将主操作统一放入左侧导航：`创建画布`、`模板库`、`创建 Key`。
- 统一仪表盘、工作流列表、模板库、登录页、个人中心、管理页及其弹窗的视觉语言。
- 为图标提供可靠的渲染与降级处理，避免图标缺失时出现空白控件。

### 个人中心和工作流页面

- 个人资料现在可以通过编辑弹窗修改，并通过 `PATCH /auth/profile` 保存。
- 重做了个人中心的顶部信息区域，移除了尺寸和配色混乱的卡片组合。
- 重做工作流列表、空状态、统计信息、创建画布和模板库弹窗，处理了小屏幕下的溢出问题。

### 画布与节点编辑

- 统一画布顶部栏、工具栏、节点选择器、表单和网关执行入口的外观。
- `通过网关执行` 已改为与其他工具栏操作一致，并修复右侧 LLM 编辑器打开时工具栏末尾没有间距、操作被遮挡的问题。
- LLM 节点改为在当前画布内打开侧边编辑器，并增加错误边界，避免点击后跳到空白页。
- 在桌面和移动尺寸下调整画布工具栏及右侧编辑器的可用空间。

### 网关与本地开发支持

- 网关本地 CORS 已允许本地回环地址，支持独立验证端口。
- 已定位默认入口的实际故障：`http://localhost:3000` 运行中的前端被旧环境值编译为请求 `http://localhost:3101`，但默认网关实际监听在 `http://localhost:3001`。因此页面会显示“无法连接网关服务”，即使 UI 文件本身已经更新。

## 已完成的验证

- 在隔离验证栈 `http://localhost:3104`（前端）和 `http://localhost:3103`（网关）中，已走查：仪表盘、模板库、创建画布、个人中心、编辑个人资料、创建 Key、管理页、画布、LLM 节点和移动端布局。
- 已检查桌面和 `390 x 844` 小屏幕下的横向溢出；个人资料、模板库、创建画布弹窗均保持在视口内。
- 已确认 LLM 点击后仍停留在画布地址，侧边编辑器正常打开；画布工具栏三项主要操作均可见，并保留末尾间距。
- 先前已通过：前端 TypeScript 检查、前端构建、网关构建、差异空白检查。
- 已确认默认网关 `3001` 可返回认证响应，且对 `http://localhost:3000` 的 CORS 预检正常。

## 正在处理

- 正在将默认入口的网关连接改成安全的本地回退机制。
- 当前工作区已把认证和通用请求收口到同一请求入口，但第一版在请求失败后会直接重试到回退网关。这个策略不适合写操作，极端网络故障下可能重复提交数据。
- 下一步会改成：先用无副作用的连通性探测确定网关地址，再只发送一次登录、保存或创建请求。

## 剩余工作

1. 完成并验证安全的 `3000 -> 3001` 本地网关回退，不重放任何写操作。
2. 在默认入口 `http://localhost:3000` 重新执行登录、工作流列表、创建画布、模板库、个人资料修改、创建 Key、画布和 LLM 节点的完整浏览器流程。
3. 在默认入口和隔离验证入口分别检查浏览器控制台错误，区分第三方开发警告与应用异常。
4. 重新执行前端 TypeScript 检查、前端构建、网关构建和差异检查。
5. 对真实“通过网关执行”保留单独的成本确认：该动作可能调用外部模型并产生费用，未经确认不主动触发。

## 已知边界

- 不会声称穷举所有输入或所有外部服务状态。验收将采用可复现的测试矩阵，覆盖关键页面、桌面/移动尺寸、正常路径、空状态、错误状态和关键跳转。
- FlowGram/Semi 的开发环境会输出一条 `findDOMNode` 弃用警告；目前它不是应用异常。仍需在最终默认入口复核是否只剩该第三方警告。
