# 待完成

> 本文件是 2026-09-22 ~ 09-23 这轮安全加固 / 依赖清零 / 测试体系整理的**任务清单与
> 完成记录**。每项均已对照代码/配置核实，非凭记忆记录。
> 优先级：**P0 = 部署前必须解决**，**P1 = 应尽快**，**P2 = 可择机**。
>
> **当前状态（2026-09-23）**：**P0 / P1 / P2 全部关闭，无遗留项。**
> 第 10 项（`demo` 账号）已按你的决定处置完毕 —— 账号已解封并恢复 admin 角色，
> 两个 GUI 测试残留账号已删除。完整 `pnpm run verify` 全链通过。
>
> ⚠️ 本文件已纳入版本管理，而 `origin` 是**公开仓库** —— 往里写内容前请先确认不含
> 密钥、真实邮箱等敏感信息（第 10 项的邮箱已打码）。

---

## 提交记录（本轮全部）

| SHA | 主题 | 文件数 |
| --- | --- | --- |
| `10e8019` | `feat(security)` 凭据随机化、数据库最小权限、全局鉴权守卫与备份恢复 | 32 |
| `4717def` | `chore(deps)` 依赖漏洞 29 → 0，含 NestJS 10→11 与 react-router v7 升级 | 6 |
| `ae14984` | `fix(mcp)` 移除裸 JwtModule 导入，MCP 不再全线 401 | 1 |
| `e5c2b97` | `feat(python)` Python 执行节点新增容器沙箱模式 | 2 |
| `63d7251` | `fix(scripts)` 补齐套件工作流清理，并同步配置与文档 | 22 |
| `259d795` | `fix(scripts)` compose 调用形式自动探测，修好 `pnpm start` 在本机跑不通 | 8 |
| `a9503b5` | `fix(seed)` 旧 demo 账号处置加幂等短路，不再每次启动重复告警 | 2 |
| `7acb72e` | `fix(draft-run)` 沙箱应用被外部删除时自愈，不再永久报 "App not found" | 2 |
| `7723df8` | `fix(knowledge)` 删除知识库时 404 不再让本地所有权行永久泄漏 | 3 |
| `7959fd9` | `test` 把 8 个从未被自动执行的测试接入 test:smoke | 1 |
| `3c885eb` | `fix(files)` 文件缺失时下载不再让网关进程崩溃 | 2 |
| `0fde691` | `chore` cnb 远端移除；媒体网关端口不一致时启动告警 | 2 |
| `bb23179` | `fix(converter)` 合并两份回连地址实现，修掉 MCP 链路的 fallback 主机错误 | 6 |
| `efb3335` | `docs` 纳入本轮任务清单与完成记录（本文件） | 1 |
| `8f065c3` | `docs` 修正本文件里的推送状态（写错了） | 1 |
| `f13f990` | `feat(scripts)` 新增 `user:reactivate`；处置 `demo` 账号与测试残留账号 | 3 |

**推送状态**：这里**故意不写静态数字** —— 见下面的说明。自己查：

```bash
git ls-remote origin refs/heads/main          # 取远端真实 SHA
git rev-list --count <那个SHA>..HEAD          # 再算领先数（0 就是已同步）
```

> ⚠️ 判断领先数**必须**用 `git ls-remote origin refs/heads/main` 取远端真实 SHA 再
> `git rev-list --count <sha>..HEAD`。`git status -sb` 里的 `[origin/main]` 只是上次
> fetch 时的缓存值 —— 本轮就因此先后报出「领先 11」和「领先 7」两个都不对的数字，
> 而实际一度是 0。
>
> **这段提示本身已经过期两次**：第一次写「`0fde691`、`bb23179` 待推」，其实它们早
> 已在远端；改成「远端 = `bb23179`，领先 1 个」之后，又因为紧接着的两个提交而过期。
> **凡是把「当前同步状态」写进文档的尝试都会立刻过期** —— 所以这里只留方法、不留结论。

> **`cnb` 远端已删除**（`git remote remove cnb`）。原因是 `git ls-remote cnb` 连接挂起、
> 完全取不到输出，属于网络不通或需交互式凭据，无法在本环境排查 —— 按你的要求不再保留
> 这个远端。现在只剩 `origin`（GitHub）。这是删除本地保存的远端**别名**，不影响 GitHub
> 上的仓库，也不影响本地代码与提交历史。

> 拆分说明：`app.module.ts`（数据库账号 + 全局守卫）、`.env.example`/`README.md`
> （多主题）、`gateway/package.json`（依赖升级 + 测试接入）本身跨主题，按文件拆不
> 干净，只能按主题凑；`docker-compose.yml` 属于第 1 项但当时漏提，已在第 5 个提交
> 里补上并标注。

### 附：媒体网关端口一致性（README 里记录的配置陷阱）—— 已彻底消掉

README 写过：改 `GATEWAY_PORT` 必须同步三处，否则媒体节点以 **Squid 403** 失败
（报错在 SSRF 代理层，与真因「一个端口没跟着改」隔得很远）。

排查中先发现一个更隐蔽的问题：**两个 bridge 各有一份回连地址实现，fallback 主机还不一样**
—— `native-media-bridge` 用 `host.docker.internal`（正确），`mcp-bridge` 用
`futureflow-gateway`（**docker 网络里不存在**，全仓库搜下来它只是 `gateway/package.json`
的 pnpm 包名）。只因 `.env` 显式钉了 `DIFY_MEDIA_GATEWAY_URL` 会短路掉 fallback 才没暴露。

三处收敛：

1. **新增 `converter/media-gateway-url.ts`** 作为唯一实现（`bb23179`）。两个 bridge 都调用它，
   并加了**源码级防回归**断言：不许再各自实现一份地址解析、不许再出现 `futureflow-gateway`。
2. **`docker-compose.yml`**：白名单端口默认值从写死 `3001` 改为
   `"${DIFY_MEDIA_GATEWAY_PORT:-${GATEWAY_PORT:-3001}}"`。实测确认 docker-compose 支持这种
   嵌套默认值，且 shell 变量优先于 `.env`。
3. **`.env` / `.env.example`**：三项改为**留空**，自动跟随 `GATEWAY_PORT`
   （`URL` 留空 → `http://host.docker.internal:${GATEWAY_PORT}`；`HOST`/`PORT` 留空 →
   compose 白名单默认值）。**这样「改 GATEWAY_PORT 要同步三处」的陷阱就不存在了。**

   实测：默认解析结果与改动前**完全一致**（`http://host.docker.internal:3001`）；
   `GATEWAY_PORT=3401` 时三处一起变成 3401。

另加一道保险：`gateway/src/main.ts` 启动时比对回连端口与监听端口，不一致就打 WARN
（只告警不阻断——反向代理场景下本就允许不同；且只在地址里写了明确端口时才比）。
实测不一致时 `WARN …回连地址指向 3001，但网关监听 3401…`，一致时 0 条。

### 附：多管理员之后的「选号陷阱」（`rotate-admin-password`）

第 10 项把 `demo` 提成了 admin 之后，按老规矩做系统性同类排查，查出这个隐患：

`rotate-admin-password.cjs` 原先用

```sql
SELECT ... FROM "users" WHERE role = 'admin' ORDER BY "createdAt" LIMIT 1
```

挑要轮换的账号 —— 也就是「**最早的** admin」。在只有一个管理员时它是对的，
而且当时「最早的」恰好就是 `admin`，所以 bug 一直被掩盖。

**一旦 `admin` 被改名或删除**，脚本会静默轮换另一个管理员的密码，
**却把新密码写进 `.env`** —— 而 `.env` 描述的是 `admin`。
两边对不上，登录直接锁死，而报错现场（登录失败）离真因（脚本挑错了账号）隔得很远。

**修法**：抽出纯函数 `selectBootstrapAdmin(admins, configuredUsername)`，
按 `.env` 的 `GATEWAY_BOOTSTRAP_ADMIN_USERNAME` **精确匹配**；
库里有管理员但配置名不匹配时**直接报错并列出所有管理员**，绝不自动挑一个。
（要轮换其他账号用 `user:reactivate`，它不碰 `.env` —— 两个脚本的分工写进了各自头部注释。）

**回归测试** `scripts/test-rotate-admin-password.cjs`（13 项，已接入 `e2e-all` 的 local 组）：

- 选择行为：精确匹配 / 多管理员时**不取数组第一项** / 配置名不存在返回 `null` /
  空与纯空白不误选 / 大小写不匹配 / **同名前缀不误选**（`admin` 不能匹配 `admin-backup`）
- 源码级防回归：不许再出现「按 role 过滤 + LIMIT 1」/ 调用点必须传配置变量而非写死 /
  报错分支必须 `throw` 而不能退化成「打印警告后继续」

**变异验证**：5 条变异全部实测报红（取第一个 / 退回 `LIMIT 1` / 前缀匹配 /
调用点写死 / 报错分支退化），还原后逐字节一致。

> 过程教训：第一版我加的变异是 `if (!wanted)` → `if (wanted === null)`，
> 结果测试没红 —— 查下来 `wanted` 经 `String(... || '').trim()` 后**不可能为 null**，
> 两者语义等价，是**假缺口**。**变异必须真的改变行为**，否则测的是「变异写错了」
> 而不是「测试守不住」。同一次里还发现我写的静态检查正则先命中了**函数定义的形参**，
> 必须先剔除定义行才能找到真正的调用点。

---

## 本次已完成（2026-09-22）

| 项 | 做了什么 | 验证方式 |
| --- | --- | --- |
| P0-1 管理员默认密码 | 新增 `scripts/lib/admin-credentials.cjs`，25 个脚本的硬编码字面量全部改为从 `.env` 读取；`init-env.cjs` 改为随机生成；`.env.example`/README 不再写具体值；新增 `scripts/rotate-admin-password.cjs` 并已在本机执行 | 25 个脚本 `node --check` 通过；全仓库仅剩 `.env` 一处存有该值；用新密码 `bcrypt.compare` 通过，`tokenVersion` 已 +1 |
| P0-2 应用数据库账号 | 新增 `infra/postgres/app-user.sql` + `initdb/10-app-user.sh`（全新卷）+ `scripts/db-grant-app-user.cjs`（存量卷，已执行）；`app.module.ts` 用 `POSTGRES_APP_USER`，迁移仍用超级用户 | 应用账号 `rolsuper/rolcreatedb/rolcreaterole` 全为 false；建角色、建库被拒；只能连 `futureflow` 库；网关用它启动不再报 `must be owner of table` |
| P0-3 依赖审计与修复 | 审计后分三轮清理：补丁级 `overrides` → 前端 `react-router` v7 + `@tiptap/core` 3.30.5、后端 NestJS 10 → 11 → `langsmith` 0.3.87 → 0.6.3。报告见 `docs/dependency-audit.md` | **`pnpm audit --prod` → `No known vulnerabilities found`（29 → 0）**；gateway/frontend `tsc` 通过；前端生产构建通过；**37 项 smoke 全绿**；网关实起并在真实 HTTP 下验证 |
| P1-3 全局鉴权守卫 | 新增 `@Public()` 装饰器 + `GlobalAuthGuard`（`APP_GUARD`），语义改为**默认要鉴权**：`@Public()` 放行 / 自带 `@UseGuards` 交回专用守卫 / 其余一律走 JwtAuthGuard。白名单先梳理后落地，共 9 条路由标 `@Public()` | `auth-coverage-smoke.ts` 改为双向对齐（漏标、标了没登记、登记了没标、守卫与 `@Public()` 并存都会红），4 种变异已实测会红；新增 `global-auth-guard-smoke.ts` 验证运行时行为；实起网关验证：公开路由 200/400、受保护路由一律 401、webhook 与 LLM 代理走到各自的处理器内校验 |
| P1-6 Webhook 密钥 | 支持 `X-Webhook-Secret` 头，路径传参保留兼容并回 `X-Webhook-Secret-Source: path-deprecated` | 新增 `gateway/test/webhook-secret-header-smoke.ts`（已接入 `test:smoke`）；实起网关验证：无密钥 400 `webhook_secret_missing`、假密钥 404、旧路径仍可用 |
| P1-7 备份与恢复 | 新增 `scripts/backup.cjs` / `restore.cjs`（`pnpm run backup` / `pnpm run restore`），覆盖两个库 + 媒体目录，带 manifest 校验 | 实跑一次备份；恢复到临时库后 `users` 行数一致；媒体目录恢复到临时目录成功 |
| P2-5 测试清理统一 | 9 个残留脚本接入 `scripts/lib/cleanup-workflows.cjs`（含按 API grep 会漏掉的 `test-page-buttons.cjs`）；顺带修掉 `test-trigger-failure-display.cjs` 的「发布失败即泄漏」 | 全部 `node --check` 通过；在真实网关上验证清理件的匹配语义（前缀/精确/不误删）；`test-page-buttons.cjs` 单独复跑确认清理生效 |
| 端到端回归 | 起 Dify 全栈 + 网关 + 前端，跑完整 `e2e-all` | **local 10/10、api 7/7、gui 7/7 → 合计 24/24** |
| **完整 `verify` 全链** | 项目自己的验证口径（`pnpm run verify`），不只是 smoke | **全绿**：`ts-check` + 7 个 runtime/archive 套件 + `test:platform`（smoke **39 项** + integration + **fuzz 21000 例**）+ `build:gateway` + `build:frontend`（rsbuild 生产构建 9.99s） |
| P2-8 复核 | 原文只标「（部分）」：`fresh-volume-e2e.cjs` 已接入，**其余脚本仍用各自 inline DELETE**。实际静态扫描确认已全部收敛 | 21 个脚本引用 `scripts/lib/cleanup-workflows.cjs`；**全仓库零 inline `DELETE FROM workflows`** —— 该项实为已完成，不是「部分」 |
| 库内残留清空 | 清掉 2 张 `按钮枚举核查-*` + MCP 服务器「验收 MCP 工具集」+ 知识库「验收知识库」 | 复核后只剩模板种子，MCP/知识库为空 |
| P1-4 Python 容器沙箱 | 新增 `PYTHON_EXEC_MODE=docker`：一次性容器执行，只读挂载本次工作目录、默认无网络、根目录只读、资源硬上限 | 新增 `python-docker-sandbox-smoke.ts`（含**真实容器**隔离实测，已接入 `test:smoke`）；真实 `/python/exec` 端到端 **8/8**；`local` 模式回归 **4/4** |
| 本机 pnpm 不可用 | 真因是 sh shim 的 `case` 只认 CYGWIN、漏了 MINGW（此前文档写的「corepack 缺 dist/pnpm.js」是**错的**）。改 `D:\nvm\v22.20.0\pnpm` 一行并备份原文件 | 修复后 Git Bash 下 `pnpm --version` → 11.10.0；`pnpm run audit:deps` / `pnpm run ts-check` 直接可跑 |
| P2-8（部分） | `fresh-volume-e2e.cjs` 已接入共享清理件 | `node --check` 通过 |
| P2-9 历史工作流 | 库里 20 条未删除的工作流中，除模板种子 `中英翻译`、`内容大纲生成` 外的 **18 条全部软删**（`status='deleted'`） | 删除前已导出 `.workbuddy-ai/backup/workflows-2026-09-22.json`；删后查询只剩那 2 条种子 |
| 附带修掉：MCP 全线 401（既有 bug） | `mcp.module.ts` 裸导入 `JwtModule`（无 `.register()`），在本模块注入器里造出第二个**没有密钥**的 `JwtService`，导致 `JwtAuthGuard` / `McpExecutionGuard` 的 `verify()` 恒抛 `secret or public key must be provided` → MCP 4 个管理接口 + `/mcp/proxy` 全部 401。移除该导入，改用 `app.module.ts` 里 `JwtModule.registerAsync({ global: true })` 全局导出的那个 | 先证明不是本轮引入：**临时摘掉全局守卫后 MCP 仍 401**（而 `/plugins` 变 200）。诊断日志坐实 `hasSecret:false, optionsKeys:[]`。修复后 6/6 通过（含「无凭据仍 401」的反向断言）；`e2e-all --group=api` **7/7** |
| 附带修掉：**文件下载可被远程触发进程崩溃** | 详见 9c-bis。`files.controller.ts` 下载路径 `createReadStream(path).pipe(res)` 无 `error` 处理，文件缺失即 uncaughtException（本项目无全局兜底）→ 网关进程直接退出。加 `stat()` 预检（缺失抛 404）+ `stream.once('error')` 兜底 | 修复前**实测进程退出**；修复后 404 且进程存活。`file-storage-smoke.ts` 补下载路径用例（含「文件缺失」放在最前，避免异步流错误掩盖真因） |
| 附带修掉：草稿沙箱丢失无法自愈（详见 9b） | `draft-run.service.ts` 无条件相信本地 `draft_sandboxes` 行；Dify 侧应用被外部删掉后永久报 `App not found`。改为走任何分支前统一校验存在性，404 即丢弃本地行并重建 | `draft-run-smoke.ts` 补 5 个用例；**变异验证**（让存在性检查恒真 → 测试报红） |
| 附带修掉：知识库删除 404 致本地行永久泄漏（详见 9c） | `knowledge.service.ts` 的 `consoleJson` 加 `tolerateMissing`，仅 `deleteDataset` 使用 | 新增 `knowledge-dataset-delete-smoke.ts`（5 组：404 继续清本地行 / 204 / 400-500-503 抛错且不清行 / 401-403 / 归属校验失败不调 Dify） |
| 附带修掉：8 个测试从未被自动执行（详见 9d） | 39 个测试文件里只有 30 个在 `test:smoke` 里，其余 9 个中 8 个的命名脚本无调用方 → 全部接入 | 8 个独立跑通后接入；`test:smoke` 链现执行 **39 项**，`test/` 下 42 个文件**全部**有归属（复核命令见 9d） |
| 附带修掉：`pnpm start` 在本机必然失败（详见 2b） | 本机无 `docker compose` 插件，只有独立 `docker-compose` v5.3.1，而脚本写死了 `docker compose`。新增 `scripts/lib/docker-compose.cjs` 探测 + `scripts/compose.cjs` 转发器 | 新增 `scripts/test-docker-compose.cjs`（15 个断言，含**静态防回归**：禁止再写死 `docker compose`）；已接入 `e2e-all` 的 local 组 |
| 附带修掉：seed 每次启动重复告警（详见第 10 项） | `disableUnsafeLegacyDemoAdmin()` 在账号已被处置后仍每次写库 + warn。加幂等短路 | `platform-smoke.ts` 补正反两个用例（已处置的不再写库 / 只降权未暂停的仍要补处置） |
| 媒体网关端口一致性（详见上节） | 新增 `converter/media-gateway-url.ts` 作为**唯一**回连地址实现，两个 bridge 都调它；`.env` 三项改留空自动跟随 `GATEWAY_PORT`；启动时比对不一致打 WARN | 新增 `media-gateway-url-smoke.ts`（含**源码级防回归**：不许各自实现、不许出现 `futureflow-gateway`）；变异验证通过；实测默认解析结果与改动前一致，`GATEWAY_PORT=3401` 时三处一起变 |
| `cnb` 远端移除 | `git remote remove cnb`（`ls-remote` 挂起、取不到输出，无法在本环境排查） | 现只剩 `origin`；不影响本地提交历史 |
| **第 10 项：`demo` 账号处置** | 按你的决定：**改密码 + 解封 + 恢复 admin 角色**（`tokenVersion` 0 → 1）。新增可复用脚本 `scripts/reactivate-user.cjs`（`pnpm run user:reactivate`），因为它与 `admin:rotate-password` 的分工容易搞混 —— 后者会重写被 25 个脚本共用的 `.env` 密码 | 登录三态实测：用户名 201 / 邮箱 201 / **旧密码 401**；token 载荷 `role:admin,status:active,tv:1`；实起网关 **0 WARN / 0 ERROR**，seed 明确走「管理员账号已存在，跳过初始化」、**未**再打「账号已暂停并降权」 |
| **第 10 项：测试账号残留** | 删除 `guitester` / `guitest891346`（全仓库零引用，手工 GUI 测试残留）。按 `information_schema` **动态枚举** 14 张子表（9 CASCADE + **5 NO ACTION**），先备份再按「先 NO ACTION、后 CASCADE、最后父行」删除 | 备份 `.workbuddy-ai/backup/accounts-purged-2026-09-23.json`；删除 1 条 `balance_logs` + 2 条 `users`；复核剩 0；库里只剩 `admin` + `demo` |
| **附带修掉：多管理员后的选号陷阱**（详见上节） | `demo` 提成 admin 后做同类排查发现：`rotate-admin-password.cjs` 用「最早的 admin」挑账号 —— `admin` 一旦改名/删除，它会静默轮换 `demo` 的密码却把新密码写进 `.env`（描述的是 `admin`）→ 直接锁死。抽出 `selectBootstrapAdmin()` 按 `.env` 用户名精确匹配，不匹配就报错列出所有管理员、绝不自动挑 | 新增 `scripts/test-rotate-admin-password.cjs`（13 项，已接入 `e2e-all` local 组 → **12/12**）；**5 条变异全部实测报红**（取第一个 / 退回 `LIMIT 1` / 前缀匹配 / 调用点写死 / 报错分支退化），还原逐字节一致 |
| **附带修掉：2b 的 compose 修复不完整**（详见 2b-bis） | 当时的静态防回归只挡了**数组形式** `spawn('docker',['compose'])`，漏了**字符串形式** `runCommand('docker compose up -d')` —— 而后者才是真正会被执行的。`pnpm run test:e2e` 在无插件机器上仍会在启动容器时失败。新增 `composeShellCommand()`，改掉 3 处调用 + 2 处报错文案；静态检查补上字符串形式 | `test-docker-compose.cjs` 15 → **19 项**全绿；**3 条变异实测报红**（退回字符串 / 去掉引号 / 写死前缀），还原逐字节一致；实测修复后命令退出码 0 |

---

## P0 — 部署到非本机环境前必须处理

### 1. ~~依赖漏洞~~ —— 已清零并完成端到端回归（2026-09-22 晚）

`pnpm audit --prod` → `No known vulnerabilities found`。三轮处置记录见
`docs/dependency-audit.md` 文末。

**端到端回归已跑完**（Dify 全栈 + 网关 + 前端都起起来）：

| 分组 | 结果 |
| --- | --- |
| local | 10/10 |
| api | 7/7（真实 LLM 执行） |
| gui | 7/7（真实浏览器） |
| **合计** | **24/24** |

这正面回答了升级最担心的两点：NestJS 11 换的 **Express 5** 没有破坏任何路由或
请求解析；`react-router` v7 与 `@tiptap/core` 3.30.5 没有破坏任何前端交互。

### 2. ~~Git Bash 下 `pnpm` 命令不可用~~ —— 已修（2026-09-22 晚）

> **此前本文的诊断是错的**（写的是「corepack 缺 `dist/pnpm.js`」）。实际
> `D:\nvm4w\nodejs\node_modules\corepack\dist\pnpm.js` **存在且可用** ——
> `node <该路径> --version` 正常输出 `11.10.0`。别照着错诊断去重装 corepack。

**真因**：`d:/nvm4w/nodejs/pnpm` 这个 sh 版 shim 里写的是

```sh
case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac
```

只认 CYGWIN，而 Git Bash 的 `uname` 报的是 `MINGW64_NT-10.0-26200`，
于是路径转换被跳过，Unix 风格的 `/d/nvm4w/nodejs/...` 被原样传给原生
`node.exe`，被当成「当前盘根目录下的 d\nvm4w\...」→
`Cannot find module 'D:\d\nvm4w\nodejs\node_modules\corepack\dist\pnpm.js'`
（注意多出来的 `d\`）。

**影响面仅限 Git Bash**：`pnpm.CMD`（用 `%~dp0`）与 `pnpm.ps1`
（用 `Split-Path $MyInvocation...`）都走 Windows 自身解析的路径，不受影响。

**已应用的修复**（改 `D:\nvm\v22.20.0\pnpm`，`d:\nvm4w\nodejs` 是它的软链；
原文件备份在同目录 `pnpm.bak-before-mingw-fix`）：

```sh
case `uname` in
    *CYGWIN*|*MINGW*|*MSYS*) basedir=`cygpath -w "$basedir"`;;
esac
```

验证：修复后 Git Bash 下 `pnpm --version` → `11.10.0`，
`pnpm --filter futureflow-gateway exec ...` 正常，`pnpm run audit:deps` /
`pnpm run ts-check` 可直接跑。**改动只有这一行**（脚本回读逐行比对确认）。

**同目录下的 `yarn`、`yarnpkg`、`corepack` 三个 sh shim 有完全相同的缺陷，也已一并修复**
（各自备份为 `<name>.bak-before-mingw-fix`，脚本回读逐行比对确认只差那一行）。
Git Bash 下实测：`pnpm --version` → 11.10.0，`yarn --version` 正常执行
（输出「本项目配置为使用 pnpm」是 corepack 的预期行为，不再是路径报错）。

> ⚠️ **`nvm install` / 切换 Node 版本会重新生成这些 shim，修复会被覆盖**，
> 重新改一次即可。四个文件的修法完全一样。

> 📌 **另有一处不在本项目范围内**：`which corepack` 实际命中的是
> **管理版 node**（`C:\Users\future\.workbuddy-ai\binaries\node\versions\22.22.2-2\corepack`，
> PATH 第 3 位，早于 nvm4w），它的 shim 也是 `*CYGWIN*)` 同款缺陷，报
> `Cannot find module 'D:\c\Users\...'`（多出 `c\`）。
> 那是 WorkBuddy 自己的运行时目录、不是你的开发工具链，**未改动**。
> 要修的话同一行改法：`*CYGWIN*|*MINGW*|*MSYS*)`。
> 注：`pnpm` / `yarn` / `yarnpkg` 不受影响 —— 管理版 node 里没有这三个，
> 命中的是 nvm4w 那份（已修）。

<details>
<summary>不依赖上述修复的备用绕法（本次实际用的）</summary>

- **本次实际用的绕法**（已验证可跑完整 install / audit）：

  ```bash
  # 1) 用隔离安装的那份 pnpm（11.10.0）
  NODE="C:/Users/future/.workbuddy-ai/binaries/node/versions/22.22.2-2/node.exe"
  PNPM="C:/Users/future/.workbuddy-ai/binaries/node/workspace/node_modules/pnpm/bin/pnpm.cjs"
  # 2) 关键：关掉宿主的批量删除保护，否则 pnpm 清理临时文件时会被拦
  CODEBUDDY_SAFE_DELETE_ENABLED=0 "$NODE" "$PNPM" install
  ```

  宿主保护的触发条件是「单会话累计 50 个删除」，pnpm 装依赖时轻易超过。
  自家终端不受此限制，所以当时没在受限会话里跑成功。

</details>

### 2b. ~~`docker compose` 在本机不可用，项目主入口跑不通~~ —— 已修（2026-09-22 晚）

**这条在 9-20 的工作日志里就记过，一直只记录未处理**，而 `pnpm start` /
`pnpm run infra:up` 正是项目主入口 —— 在这台机器上必然失败。

- **现象**：`docker compose ...` 报 `docker: unknown command: docker compose`。
- **真因**：本机 Docker Desktop **没装 compose 插件**（`docker info --format
  '{{json .ClientInfo.Plugins}}'` 的列表里只有 `dhi`），只有独立二进制
  `docker-compose`（v5.3.1，在 `Docker/resources/bin/` 下）。
  两种形式的**参数完全一致**，只是调用前缀不同。
- **原先的写法**：`start-full-stack.cjs`、`fresh-volume-e2e.cjs` 与 `package.json`
  的 4 个脚本都**写死** `docker compose`。
- **修法**：新增 `scripts/lib/docker-compose.cjs` 做探测（优先插件、回退独立二进制、
  两者都无时给可读提示），`scripts/compose.cjs` 作为 CLI 转发器供 package.json 使用。
  两个脚本改走探测结果，`package.json` 的 `db:up`/`compose:up`/`db:down`/`db:logs`
  改为 `node scripts/compose.cjs ...`。

**过程中自己引入又修掉的一个回归**（值得记）：给 `captureDocker` 加
`shell: true` 后，`--format '{{json .State}}'` 被 cmd 按空格拆成两个参数，docker 报
`template parsing error: unclosed action` → 健康检查永远读不到 `Health` 字段 →
`infra:up` 卡 90 秒后报「postgres 未在预期时间内健康」——**报错与真因毫不相干**。
已去掉 shell（`spawnSync` 不带 shell 时 Windows 会自动补 `.exe`，够用）。

**验证**：
- 新增 `scripts/test-docker-compose.cjs`（已接入 `e2e-all` 的 local 组）：探测逻辑、
  前缀解析、转发器退出码透传、**真实容器 State 读取**，以及**静态防回归** ——
  禁止 `package.json` 与 `scripts/*.cjs` 再出现写死的 `docker compose`。
- 两次变异验证都实测报红：植入写死的 compose 调用 → 静态检查红；重新植入
  `shell: true` → `containerState` 检查红（`实际 null`）。
- **`pnpm run infra:up` 完整跑通**：compose up → 健康检查 → Dify 初始化 →
  数据库迁移 → 「The full container stack is ready」。
- `pnpm run db:up` / `db:logs` 直接可用。

> 这与 pnpm 那个问题无关，是两个独立的环境事实。

#### 2b-bis. 🔴 上面这次修复**不完整** —— 已补齐（2026-09-23）

第 10 项做完后按老规矩做全仓库同类排查，发现当时的静态防回归只挡住了**数组形式**：

```js
spawn('docker', ['compose', 'up', '-d'])     // ← 当时挡住了
```

而漏了**字符串形式** —— 恰恰是真正会被执行的那种：

```js
await runCommand('docker compose up -d', ...)   // e2e-full-test.cjs（shell: true）
execSync('docker compose down -v', ...)         // e2e-test-with-retry.cjs
```

**后果**：`pnpm run test:e2e`（→ `e2e-full-test.cjs`）在没装 compose 插件的机器上，
会在「启动容器」这一步直接失败，报 `docker: unknown command: docker compose` ——
**正是 2b 要修的那个坑，只是换了个入口**。

**补齐内容**：

1. `lib/docker-compose.cjs` 新增 `composeShellCommand(args)`，供确实需要 shell
   字符串的场景使用；含空白/shell 元字符的参数会被引号包起来
   （`{{json .State}}` 里那个空格裸露就会被 shell 拆开 —— 2b 自己踩过的坑）。
2. `e2e-full-test.cjs`、`e2e-test-with-retry.cjs`、`start-full-stack.cjs` 的
   `docker compose logs <svc>` 提示文案，全部改走它。
3. `db-grant-app-user.cjs` / `rotate-admin-password.cjs` 的报错文案从
   `docker compose up -d postgres` 改为 **`pnpm run db:up`**（那条链路自带探测）。
4. **静态防回归补上字符串形式**：新增一条检查
   `scripts/*.cjs 不再把 compose 调用写成 shell 字符串`（正则要求引号/反引号
   紧跟 `docker compose` + 空白，所以说明文字里的 `` `docker compose` `` 不会误伤）。
5. 顺带补 `composeShellCommand` 的三个行为测试（本机形式 / 空白参数加引号 / 简单参数不加引号）。

**验证**：`scripts/test-docker-compose.cjs` 15 → **19 项**全绿；3 条变异实测报红
（退回字符串形式 / 去掉引号 / 写死 `docker compose`），还原逐字节一致；
实测修复后的命令在本机可执行：

```
修复前: docker compose up -d        → docker: unknown command: docker compose
修复后: docker-compose up -d        → 退出码 0，容器列表正常返回
```

> ⚠️ **没有跑完整的 `pnpm run test:e2e`**：该脚本开头就 `compose down --remove-orphans`
> 再 `up -d`，会把整个栈拆了重启（含 Dify），耗时且会打断当前环境状态。
> 本次只验证了「被修的那一处」——即 compose 调用拼装正确且可执行。
> 其余逻辑未改动。

---

## P1 — 安全性 / 健壮性

### 3. ~~没有全局鉴权守卫，靠人工记得挂~~ —— 已处理（2026-09-22 晚）

白名单先梳理、再落地，没有一刀切。判定顺序见 `src/common/guards/global-auth.guard.ts`：

1. `@Public()` → 放行（这类路由必须在 `auth-coverage-smoke.ts` 的 `PUBLIC_ROUTES` 登记理由）；
2. 处理器/控制器自带 `@UseGuards(...)` → 放行，交回专用守卫（MCP 回调令牌、媒体执行令牌
   各有各的凭据，不能被 JWT 一刀切覆盖）；
3. 其余 → 一律走 `JwtAuthGuard`。

已标 `@Public()` 的 9 条路由：登录、注册、`/workflows/run`（已停用）、`:id/execute`、
`:id/draft-run`（后三条由 AuthMiddleware 覆盖，支持 JWT 与平台 API Key 两种身份）、
`/workflows/health`、`GET /healthz`、webhook 触发器入口、LLM 代理
`chat/completions`（处理器内校验票据）。

遗留：仍**没有 ThrottlerModule**，限流是各端点自己实现的（登录、注册、webhook 等）。

### 4. ~~Python 执行端点：环境白名单不等于沙箱~~ —— 已加容器沙箱模式（2026-09-22 晚）

新增 `PYTHON_EXEC_MODE=docker`：把用户代码放进一次性容器里跑。加固项逐条见
`buildPythonDockerArgs`，实测效果对比：

| 能力 | `local`（默认） | `docker` |
| --- | --- | --- |
| 读宿主 `.env` | **能读** | 读不到（只只读挂载本次临时工作目录） |
| 网络 | 可达 | `--network=none`，完全无网络 |
| 根文件系统 | 可写 | 只读（仅 `/tmp` 是 noexec tmpfs） |
| 资源上限 | 仅 15s 超时 + 限流 | 另加 256 MB / 1 CPU / 128 进程 |
| 残留进程 | Windows 上正常结束可能留下 | 容器随执行消失 |

其余加固：`--cap-drop=ALL`、`no-new-privileges`、`--user 65534`（非 root）、
挂载一律 `readonly`。挂载用 `--mount type=bind,...` 而非 `-v`：后者用冒号分隔，
而 Windows 盘符自带冒号，语义有歧义。

**默认仍是 `local`** —— 现有部署的 Python 节点可能靠网络连库（内置「查询 PostgreSQL」
模板就是），默认切 docker 会直接打断这类用法。

验证（不是只跑通，是逐项证明隔离成立）：
- `python-docker-sandbox-smoke.ts`（已接入 `test:smoke`）：参数断言 + **真实容器**里
  实测宿主文件不可读 / 网络 blocked / 根目录只读 / `/tmp` 可写 / 凭据环境变量 0 个 / uid=65534。
- 走真实 `/python/exec` 端点的端到端验证 **8/8**：普通计算正常、中文正常、读不到宿主
  文件、无网络、根目录只读、随仓库携带的 pg8000 可用（走 `/vendor` 挂载）、超时被拦截
  （16.0s / 15s 上限）、**超时后无残留容器**。
- `local` 模式回归 **4/4**：功能未变、环境变量白名单仍生效、仍能读宿主文件
  （确认隔离没被误加到默认路径上）、未创建任何容器。

> 说明：这是「容器 + 只读挂载」这一档的隔离，不是 gVisor 那种内核级隔离。
> 对「防误读宿主凭据、防网络横移、防资源耗尽」够用；若威胁模型要求对抗恶意逃逸，
> 还需 gVisor/Kata 之类的运行时。

### 5. ~~NestJS 11 / Express 5 的端到端回归~~ —— 已跑（2026-09-22 晚）

`e2e-all` 三组全绿，**24/24**。详见上面 P0-1。
`pnpm run verify` 也已跑（含 `ts-check`、各运行时套件、双端构建）。

### 5b. ~~MCP 全线 401（既有 bug，非本轮引入）~~ —— 已修（2026-09-22 晚）

- **现象**：MCP 的 4 个管理接口 + `/mcp/proxy` 全部返回 401「无效或过期的 Token」。
  因为 `McpExecutionGuard` 也注入同一个坏掉的 `JwtService`，**工作流里的 MCP 节点
  整条链路都不可用**。
- **根因**：`mcp.module.ts` 裸导入了 `JwtModule`（无 `.register()`）。裸模块只声明
  `providers: [JwtService]`，会在 McpModule 自己的注入器里造出第二个 `JwtService`；
  而 `JwtModule.registerAsync()` 只提供 `JWT_MODULE_OPTIONS`、**不导出它**，
  所以这个实例的 `options` 是空对象 → `verify()` 恒抛
  `secret or public key must be provided`。
- **排除「是本轮引入」的过程**：临时摘掉全局守卫后 MCP 仍 401（同时 `/plugins` 变 200，
  反证全局守卫本身工作正常）→ 说明与 P1-3 无关。再用诊断日志坐实
  `hasSecret:false, optionsKeys:[]`。
- **修法**：从 `mcp.module.ts` 的 `imports` 移除裸 `JwtModule`，改用
  `app.module.ts` 里 `JwtModule.registerAsync({ global: true })` 已全局导出的那个。
  全仓库排查过，只有这一处是裸导入。
- **验证**：MCP 修复验证 6/6（含「无凭据仍 401」的反向断言，确认鉴权没被放开）；
  `e2e-all --group=api` 从 6/7 变成 **7/7**。

> 教训：`@nestjs/jwt` 的裸 `JwtModule` 是个陷阱 —— 它能让应用正常启动，
  只在运行时把该模块内的令牌校验全部打回 401。凡是在某个模块里用
  `@UseGuards(JwtAuthGuard)` 或注入 `JwtService`，都要确认这个模块**没有**
  再 import 裸 `JwtModule`。

---

## P2 — 测试体系遗留

### 6. ~~其余测试脚本未接入统一清理~~ —— 已处理（2026-09-22 晚）

8 个脚本全部接入 `scripts/lib/cleanup-workflows.cjs`，现在共 19 个脚本共用一份清理逻辑。

| 脚本 | 匹配方式 | 说明 |
|---|---|---|
| `test-version-management.cjs` | 前缀 `版本验收-` + 精确 `导入验收工作流` | 原「只删主流程那张」，导入那张崩在两步之间就泄漏 |
| `test-complex-workflow.cjs` | 前缀 `诗词 API 验收-` / `复杂工作流验收-` | 两处 finally 都改用共享件 |
| `api-test.cjs` | 前缀 `E2E Test Workflow` | **保留** `DELETE /workflows/:id`（那是端点测试 Test 14），只补兜底清理 |
| `e2e-full-test.cjs` | 精确 `E2E Test Workflow` | 清理插在停网关**之前**（顺序反了就没服务可连） |
| `e2e-test-with-retry.cjs` | 前缀 `E2E Workflow` | 放在**每轮** finally，失败重试时上一轮残留也带走 |
| `test-draft-run-online.cjs` | 精确 `云端试运行验收` | 知识库另走显式 DELETE（共享件只覆盖工作流） |
| `test-task-center.cjs` | 前缀 `任务中心验收-` | 批量任务名不匹配工作流前缀，不会误伤 |
| `test-trigger-failure-display.cjs` | 前缀 `连续失败展示-` | **保留**触发器删除（遗留触发器会吃满并发名额），并修掉「发布失败即泄漏」 |

统一到共享件的主要收益不是「少写几行」，而是：**单删只能收拾「跑到那一行」的那一次**，
套件中途崩掉留下的残留会一直堆在用户的工作流列表里；共享件是按名字扫描，
下次跑就能把上次的残留一起带走。

顺带修掉的泄漏点：
- `test-trigger-failure-display.cjs`：发布/挂触发器在 `try` 之外，失败即泄漏 → 已挪进 try。
- `test-page-buttons.cjs`：**原来完全没有清理**，每跑一次就在用户列表里留一张
  `按钮枚举核查-*`（实测已堆 2 张）→ 已接入共享件（前缀 `按钮枚举核查-`）。
  这个脚本是通过 GUI 建画布的，按「API 调用」grep 会漏掉它，是跑完端到端后
  核对库内残留才发现的。
- 各脚本的 `catch` 里都补了清理调用（脚本用 `process.exit`，`finally` 不保证执行）。

验证：脚本全部 `node --check` 通过；另写了一次性功能验证脚本，在真实网关上确认
共享清理件的匹配语义正确 —— 前缀命中 1/删 1、精确命中 1/删 1、名字不匹配时一张不动、
**不误删对照数据**、跑完无残留。`test-page-buttons.cjs` 改完单独跑了一遍确认清理生效
（29/29 通过 + 「匹配 1 个，删除 1 个」）。验证脚本用完即删，不入库。

### 7. ~~库里仍有归属不明的历史工作流（19 条）~~ —— 已处理（2026-09-22）

按「都是测试数据，不要了」全部软删，只保留模板种子 `中英翻译`、`内容大纲生成`。
原始行已导出到 `.workbuddy-ai/backup/workflows-2026-09-22.json`（另有 9-21 那份
更早的备份）。需要恢复就把 `status` 改回 `active`。

> 注：这些都是**软删**，物理行还在。

### 8. ~~库内残留~~ —— 已清空（2026-09-22 晚）

跑完端到端后核对了一次库内残留，清掉：

| 资源 | 来源 | 处理 |
|---|---|---|
| `按钮枚举核查-50794` / `-24658` | `test-page-buttons.cjs`（本轮 gui 跑了两轮） | 软删（并修好脚本，见上） |
| MCP 服务器「验收 MCP 工具集」 | 9-15 的旧测试，指向已失效的 `127.0.0.1:3921` | 硬删（已确认） |
| 知识库「验收知识库」（含 2 文档） | 旧 GUI 测试 | 硬删（已确认） |

现在库里只剩模板种子 `中英翻译`、`内容大纲生成`，MCP 与知识库均为空。

### 9. ~~Dify 侧孤儿应用~~ —— 已清理（2026-09-22 晚）

**上一版本文里给的方法（「再走一次 `DELETE /workflows/:id`」）是不可行的** ——
`workflow-crud.service.ts:57` 对已软删的工作流直接抛 404「工作流不存在」。
下面是实测出来的真实情况。

**规模比想象的小**：Dify 里只有 **19 个应用**，不是 1111 —— 库里那 1111 条软删工作流
绝大多数从未导入过 Dify。

| 侧 | 清理前 | 清理后 |
| --- | --- | --- |
| `dify_integrations` | 18 行（1 存活 / 14 指向已软删 / 3 指向已不存在） | **1 行**（`default`，全局授权） |
| Dify `apps` | 19 个 | **1 个**（`futureFlow Bridge`） |

**成因**：`WorkflowCrudService.delete()` 本身**会**调
`difyIntegration.deleteWorkflowIntegrations()` 清理 Dify 资源，但之前的批量软删是
**直接改 SQL** 绕过了服务方法，所以 Dify 侧没跟着清。

**做法**：先导出清单到 `.workbuddy-ai/backup/dify-orphans-2026-09-22.json`（含保护名单、
分组、appIds），再**复用网关自己的 `DifyIntegrationService`**（认证解析、令牌刷新重试、
404 视为已删、绑定行清理都在那里，另写一套只会分叉）。脚本内置两道保险：
保护名单为空则中止；待删清单与保护名单有交集则中止。

结果 **13/13 组、18 个应用**全部清理成功。`futureFlow Bridge` 已确认保留
（它是 dify-init 建的全局桥接应用，`.env` 的 `DIFY_API_KEY` 就是它）。

> ⚠️ **但我在这步犯了一个错，并因此修出一个真 bug** —— 详见 9b。

#### 9b. 误删草稿沙箱应用，并修出「沙箱丢失无法自愈」的真 bug

**我的错**：判断「无引用应用」时，我只查了 `dify_integrations` 一张表，
**漏查了 `draft_sandboxes`**。于是把 `a508a062-…`（其实是**草稿沙箱应用**）当成
「无任何引用」删掉了。

**后果**：`draft-run-online`、`task-center`、`complex-workflow` 三个套件从通过变成
**每次都以 `Dify 草稿沙箱请求失败（HTTP 400）："App not found"` 硬失败**
（完整端到端从 24/25 掉到 22/25）。

**这暴露了一个真 bug（比误删本身更重要）**：`DraftRunService` 无条件相信
`draft_sandboxes` 那一行 —— 只要 Dify 侧的应用被外部删掉（人工清理、数据卷重置、
或像我这样误删），该用户的草稿试运行就会**永久**坏着，DSL 不变就永远不自愈。
而且**两条路径都会用到 `existing.appId`**：
- DSL 未变 → 直接复用（`return { appId: existing.appId, ... }`）
- DSL 变了 → 仍复用同一个应用重新导入（`existing?.appId || createApp(...)`）

我第一次的修复只堵了前者，重跑仍然失败 —— 因为测试每次都是新 DSL，走的是后者。

**修法**：在读取记录后、走任何分支之前，统一做一次存在性校验
（`sandboxUsable()` → `GET /apps/:id`，只有明确的 404 判定为丢失），
丢失就删掉陈旧记录并重建。两个细节：
- **只有 404 算丢失**：网络抖动/5xx 一律按「存在」处理，否则一次瞬时故障就会被
  当成应用丢失、白白重建沙箱（导入 + 发布 + 换 Key，代价远大于一次 GET）。
- **拿不到 Console 授权时跳过校验并放行**：复用路径本来就不需要授权（执行用的是
  已存的 Service API Key），不能因为控制台暂时不可用而把能跑的路径拖垮。

**验证**：
- 真实自愈：特意**不手动清理**那条陈旧记录，重启网关后跑 `draft-run-online` ——
  日志出现 `WARN [DraftRunService] 草稿沙箱应用 a508a062-… 在 Dify 侧已不存在，
  丢弃这条记录并重建沙箱`，套件 **12/12** 通过，记录已指向新应用 `0c3eb8cd-…`。
- `draft-run-smoke.ts` 补了 5 个用例：陈旧+DSL变、陈旧+DSL未变、健康+DSL未变（复用
  且不调导入发布）、健康+DSL变（同应用重导、不新建）、拿不到授权时照常复用。
- 变异验证：把 `sandboxAppExists` 改成永远返回 true（模拟「检测不到丢失」），
  测试实测报红并给出断言文案。
  （第一次变异写错了 —— 把判断改成 `if (false)` 会破坏 TS 类型收窄、直接编译失败，
  那次「通过」是假的；换成不破坏类型的变异才有效。）

#### 9c. 顺带修掉同类模式：知识库删除的 404 会让本地行永久泄漏

误删事件之后，我把「哪些表引用了 Dify 资源」全量枚举了一遍（见下表），确认影响面只有
草稿沙箱一处，并顺手修掉同一类问题。

| 列 | 状态 |
| --- | --- |
| `dify_integrations.appId` | 已清理 |
| `draft_sandboxes.appId` | 已修（见 9b） |
| `knowledge_dataset_owners.datasetId` | **发现同类问题，已修** |
| `workflow_runs.difyWorkflowId` / `difyTaskId` | 只写不读的历史记录，无风险 |

**问题**：`deleteDataset()` 先调 Dify 删除、再清本地所有权行。Dify 侧返回 404 时
（知识库被外部删掉，或上一次删除在清本地行之前中断），`consoleJson` 会抛错 →
**本地所有权行永远清不掉**，而且之后连重试删除都清不掉（每次都在同一处抛错，
永远走不到清理那一步）。

**修法**：给 `consoleJson` 加一个显式的 `tolerateMissing` 选项（只在删除这类幂等
操作上用），404 时返回 `undefined` 让流程继续收尾。**只有 404 被容忍** ——
400/500/503 照旧抛错，否则真实的删除失败会被伪装成成功，用户以为删掉了、实际还在，
比清不掉本地行更糟。

**验证**：新增 `knowledge-dataset-delete-smoke.ts`，5 组用例：404 必须继续清本地行、
204 正常清、400/500/503 必须抛 `BadRequest` 且不清行、401/403 抛
`ServiceUnavailable` 且不清行、归属校验失败时不调 Dify、管理员可删他人知识库。
变异验证：摘掉 404 容忍后测试实测报红。

#### 9c-bis. 🔴 文件下载可被远程触发进程崩溃（**本轮最严重的一处**）

排查「本地记录说资源在、实际已不在」这一类时发现的。

**问题**：`files.controller.ts` 的下载处理是

```ts
res.setHeader('Content-Length', String(record.sizeBytes));
createReadStream(absolutePath).pipe(res);      // ← 没有 error 处理
```

`FileStorageService.open()` 只查 DB 行与路径安全性，**不检查物理文件是否存在**。
文件缺失时 `createReadStream` 会发出一个**没人处理**的 `'error'`，在 Node 里等同于
`uncaughtException` —— 而本项目**没有全局 uncaughtException 兜底**（也没有全局异常
过滤器），结果是**网关进程直接退出**。

**已实测确认**（不是推理）：

| | 修复前 | 修复后 |
| --- | --- | --- |
| 请求结果 | 连接被断（`fetch failed`） | `404 {"message":"文件内容已丢失，请重新上传"}` |
| 网关进程 | **退出**（日志里是裸的 `ENOENT`） | 存活，其他接口正常 |

DB 行在、文件不在并不罕见：手工清盘、或从备份恢复时媒体目录不完整，都会造成这种
状态。**任何能登录的用户只要命中一条这种记录，就能把网关打挂。**

**修法**（两层，与 `media.controller.ts` 的 Range 下载保持一致）：
1. 发响应头**之前**先 `stat(absolutePath)`，缺失就抛 404 —— 干净的错误，客户端能看懂
2. 再给流挂 `stream.once('error', reject)` 作为兜底 —— 挡住 `stat` 通过后、读取前
   文件被删掉的 TOCTOU 窗口

**验证**：走真实 HTTP 的端到端 **7/7** —— 上传正常、正常下载内容一致、删掉物理文件后
返回 404、**进程存活**、其他接口仍正常。

变异验证：回退到修复前的写法后，网关进程**实测崩溃**（后台任务消失、日志出现裸
`ENOENT`）；测试用例也报出 `Missing expected rejection: 物理文件缺失必须返回 404`。

> 顺带确认：`media.controller.ts` 的同类下载**写法是对的**（`stream.once('error', reject)`
> + 等待 finish/close），不需要改。只有 files 这一处漏了。

#### 9d. ~~9 个测试套件存在但从未被自动执行~~ —— 已全部接入（2026-09-22 晚）

一开始只是发现 `pnpm run test:knowledge` 没有调用方，顺着查下去发现范围更大：

**39 个测试文件里，只有 30 个在 `test:smoke`（`verify` 唯一会跑的单测入口）里。**
其余 9 个各自挂在命名脚本下，而其中 **8 个的命名脚本没有任何调用方**：

| 测试 | 独立运行 | 原状态 |
| --- | --- | --- |
| `cron-parser` / `draft-run` / `fail-branch` / `file-storage` | PASS 4–11s | ❌ 无人调用 |
| `login-rate-limit` / `mcp-bridge` / `subworkflow` / `trigger-daily` | PASS 5–8s | ❌ 无人调用 |
| `batch-loop` | — | ✅ 被 `test:batch-loop` 引用，而它在 `verify` 里 |

8 个都是**纯单测**（桩依赖、不需要外部服务），全部独立跑通。也就是说
「失败分支 / 文件存储 / 登录限流 / MCP 桥 / 子工作流 / 定时触发 / cron 解析 /
草稿试运行编排」这几块**一直没有回归防护** —— 改坏了 `verify` 也不会报。

已全部接入 `test:smoke`，现在 `test/` 下的测试文件**全部**有归属。

**完整账目**（`test/` 共 42 个 `.ts`，其中 40 个 `*-smoke.ts`）：

| 入口 | 数量 | 说明 |
| --- | --- | --- |
| `test:smoke` 直接引用 | 36 | 末尾再调 `test:media` |
| ↳ `test:media` | 3 | `media-provider` / `native-media-bridge` / `media-gateway-url` |
| ↳ `test:batch-loop` | 1 | `batch-loop-smoke`，被 `verify` 调用 |
| ↳ `test:integration` / `test:fuzz` | 2 | `platform.integration.ts` / `platform-fuzz.ts` |
| **`test:smoke` 链实际执行** | **39** | 36 + 3 |
| **`verify` 覆盖** | **42/42** | 上面四条加起来即全部 |

> 复核命令（本轮又跑了一次，结论不变）：
> ```bash
> cd gateway && node -e "
> const fs=require('fs'),p=require('./package.json').scripts;
> const all=fs.readdirSync('test').filter(f=>f.endsWith('.ts'));
> const get=k=>(p[k].match(/test\/[\w.-]+\.ts/g)||[]).map(s=>s.replace('test/',''));
> const cov=new Set([...get('test:smoke'),...get('test:media'),...get('test:batch-loop'),...get('test:integration'),...get('test:fuzz')]);
> console.log('未归属:', all.filter(f=>!cov.has(f)).join(', ')||'(无)');"
> ```

> 特别值得一提：`draft-run-smoke.ts` 也在其中 —— 9b 里刚给它加的 5 个用例
> 原本也不会被 `verify` 跑到。

> 排查方法（可复用）：**别只看一层引用**。逐个测试文件查是否被 `package.json` 引用，
> 再查「引用它的那个 script」本身是否被引用 —— 间接孤儿就是这样漏掉的。
> 命令：
> ```bash
> # 列出不在 test:smoke 里、且其命名脚本也无人调用的测试
> node -e "const fs=require('fs');const p=JSON.parse(fs.readFileSync('package.json','utf8')).scripts;const s=p['test:smoke']+' '+p['test:media'];console.log(fs.readdirSync('test').filter(f=>f.endsWith('smoke.ts')&&!s.includes(f)))"
> ```

> 查 Dify 库用的是 `dify` 账号（不是 `postgres`）：
> `docker exec futureflow-dify-postgres psql -U dify -d dify -Atc "..."`

### 10. ~~`demo` 账号被安全机制锁定~~ —— 已按你的决定处置（2026-09-23）

清理时发现库里有个 `demo` 账号，状态是 `suspended` + 角色降为 `user`。

- **不是本轮造成的**：`updatedAt = 2026-09-16T07:22:37Z`，账号在 9-16 就被锁了。
- **锁它的原因**：该账号仍在使用历史公开密码 `demo123456`，
  `SeedService.disableUnsafeLegacyDemoAdmin()` 检测到后暂停并降权（这是 P0-1 安全
  加固的一部分，设计如此 —— 公开密码等于任何人可登录）。
- **邮箱是 `132****@qq.com`**（本文件已入库，此处打码）。它与你 git 的
  `user.email` 一致 —— 所以这是**你自己的账号**，不是别人的，因此交由你决定。

**你的决定与执行结果**：

| 账号 | 处置 | 结果 |
| --- | --- | --- |
| `demo` | **改密码 + 解封 + 恢复 admin 角色** | `role=admin`、`status=active`、`tokenVersion` 0 → 1（封禁前签发的 JWT 立即失效）；密码已换为随机值，只显示一次、不落盘 |
| `guitester` | **删除** | 全仓库零引用（手工填注册表单的残留）；名下 1 条 `balance_logs` 一并删除 |
| `guitest891346` | **删除** | 全仓库零引用；名下无任何数据 |

**为什么不能复用 `rotate-admin-password.cjs` 改 `demo`**：那个脚本作用于
`WHERE role='admin' ORDER BY "createdAt" LIMIT 1`，且会**重写 `.env` 里的
`GATEWAY_BOOTSTRAP_ADMIN_PASSWORD`** —— 而那个值被 25 个测试脚本共用。
拿它改 `demo` 既改不到目标账号，还会污染测试凭据。所以新增了专用脚本：

```bash
pnpm run user:reactivate --username demo --role admin   # 支持 --dry-run / --password
```

它把「换密码 → 恢复状态与角色 → `tokenVersion` +1」三步一次做完。
**第三步最容易漏**：封禁只是让鉴权层拒绝旧 token，旧 token 本身并没到期，
不自增 `tokenVersion` 就等于留了一把还能开的钥匙。

**删除为什么必须动态枚举子表**：`users` 有 14 张子表，其中 9 张 `CASCADE`、
**5 张 `NO ACTION`**（`workflows` / `workflow_runs` / `workflow_triggers` /
`api_keys` / `balance_logs`）—— 不先删这些子行，整条删除会直接失败。
删除脚本按 `information_schema` **动态取表名**（不写死），先导出备份再按
「先 NO ACTION、后 CASCADE、最后父行」的顺序删。这是上轮「判断有无引用必须
枚举所有可能引用它的表」那条教训的直接应用。

**不会复发**：`disableUnsafeLegacyDemoAdmin()` 是**按密码判定**的
（`bcrypt.compare(旧公开密码, passwordHash)`），换了新密码后它自动短路，
下次启动不会再封。已实起网关验证（日志无「账号已暂停并降权」，登录可通）。

- 顺带修掉了这条逻辑的一个小缺陷：原实现在账号**已被处置过**之后仍会在每次启动
  重新写库并打 warn，反复出现的「账号已暂停并降权」会被读成「刚刚又被锁了一次」。
  现在加了幂等短路（并补了正反两个用例：已处置的不再写库、只降权未暂停的仍要补处置）。

- **顺带发现并清掉的残留**：库里原有的 `guitester` / `guitest891346` 两个账号
  全仓库零引用，是手工 GUI 测试留下的。备份见
  `.workbuddy-ai/backup/accounts-purged-2026-09-23.json`。

---

## 附：本次新增的运维入口

```bash
pnpm run admin:rotate-password     # 轮换**引导管理员**密码（改 .env + 数据库 + 旧 JWT 失效）
pnpm run user:reactivate --username <名> --role admin   # 解封任意账号：换密码 + 恢复状态角色 + tokenVersion+1
pnpm run db:grant-app-user         # 为存量卷创建/刷新受限应用数据库账号
pnpm run backup / pnpm run restore # 两库 + 媒体目录的备份与恢复
pnpm run audit:deps                # pnpm audit --prod
pnpm run ts-check                  # 双端类型检查
pnpm run test:e2e-all              # 24 项端到端（分层：--group=local|api|gui）
pnpm run verify                    # 完整验证链（类型 + 单测 + 集成 + fuzz + 双端构建）
```

> `admin:rotate-password` 与 `user:reactivate` 的分工别搞混：前者只作用于
> **引导管理员**（`role='admin'` 里最早创建的那个），并且会重写 `.env` 里的
> `GATEWAY_BOOTSTRAP_ADMIN_PASSWORD` —— 那个值被 25 个测试脚本共用。
> 要改**其他**账号一律用 `user:reactivate`，它不碰 `.env`。

> 如果在本机跑这些命令时遇到 `SAFE_DELETE_BULK_CONFIRM_REQUIRED`（宿主批量删除
> 保护，`pnpm install` 与前端 `rsbuild build` 会触发），在命令前加
> `CODEBUDDY_SAFE_DELETE_ENABLED=0`。自家终端不受此限制。

> ⚠️ **用 `curl` 探活必须加 `--noproxy '*'`**：本环境注入了
> `http_proxy=http://127.0.0.1:10245`，curl 会把 `localhost:3001` 也塞给代理，
> 拿回一个**假 502**（服务明明没起，看起来却像「起了但报错」，排查方向直接被带偏）。
> 判据：`curl -w '%{http_code}'` 输出 `000` 才是真·不通。
> Node 不受影响（原生 `fetch` / `axios` 默认不读 `http_proxy`），所以测试脚本没问题，
> **只有手工 curl 会踩**。

## 附：本次新增/改造的测试

```bash
pnpm --filter futureflow-gateway run test:smoke   # 39 项（36 直接 + 3 经 test:media）
```

| 测试 | 作用 |
| --- | --- |
| `gateway/test/auth-coverage-smoke.ts` | 源码级清点：控制器/路由必须能归类为「有守卫」「自带守卫」「`@Public()` 且登记理由」；`@Public()` 与清单双向对齐 |
| `gateway/test/global-auth-guard-smoke.ts` | 全局守卫**运行时**行为：`@Public()`/自带守卫放行且不叠加 JWT，裸路由一律 401 |
| `gateway/test/webhook-secret-header-smoke.ts` | webhook 密钥走头、旧路径兼容标记、缺密钥 400 |
| `gateway/test/python-docker-sandbox-smoke.ts` | 容器沙箱：加固参数齐全 + 工作目录只读 + 不挂宿主目录；**真实容器**里实测宿主文件不可读 / 无网络 / 根目录只读 / `/tmp` 可写 / 非 root（无 docker 或镜像时自动跳过实跑，参数断言仍执行） |
| `gateway/test/media-gateway-url-smoke.ts` | 回连地址唯一实现：显式值优先 / 去尾斜杠 / fallback 主机必须是 `host.docker.internal` / 端口三级优先级 / 空白值等价于未设置 / 非法地址拒绝；**源码级防回归**（不许各自实现、不许出现 `futureflow-gateway`） |
| `gateway/test/knowledge-dataset-delete-smoke.ts` | 知识库删除：Dify 侧 404 必须继续清本地所有权行；400/500/503 抛 `BadRequest` 且不清行；401/403 抛 `ServiceUnavailable`；归属校验失败不调 Dify |
| `gateway/test/draft-run-smoke.ts`（补 5 例） | 草稿沙箱在 Dify 侧丢失时自愈：只有 404 判丢失、拿不到授权时放行不误删 |
| `gateway/test/file-storage-smoke.ts`（补下载路径） | 文件下载：正常流式返回；**文件缺失时 404 而非进程崩溃** |
| `gateway/test/platform-smoke.ts`（补 2 例） | 旧 `demo` 账号处置幂等：已处置的不再写库；只降权未暂停的仍要补处置 |
| `scripts/test-docker-compose.cjs` | compose 探测与回退；**静态防回归**：`package.json` 与 `scripts/*.cjs` 不许再写死 `docker compose` |

> 另有 8 个「原本从未被自动执行」的既有测试（`cron-parser` / `draft-run` /
> `fail-branch` / `file-storage` / `login-rate-limit` / `mcp-bridge` / `subworkflow` /
> `trigger-daily`）已全部接入 `test:smoke`，详见 9d。

> 端到端另有两层：`node scripts/e2e-all.cjs`（24 项，见 P0-1）；
> 以及本轮为验证清理件与沙箱而写的一次性脚本（用完即删，不入库）。
